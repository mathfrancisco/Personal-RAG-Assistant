# Retro — 2026-07-16 · Fases 1→3 (Ingestão, Retrieval, Geração)

## 1. Objetivo da sessão
Continuar a implementação a partir do último ponto (Fase 0 commitada, Fases 1-3
parciais/não commitadas) e fechar até ter `rag ingest`, `rag search` e `rag ask`
funcionando com testes verdes.

## 2. Decisões-chave
- **Fase 1 estava quase pronta** — faltava só o teste de contrato do vector store.
  Adicionado `tests/integration/test_vectorstore.py` contra Chroma real (tmp_path),
  cobrindo upsert/query/delete/known_sources + `EmbeddingMismatchError` (SDD §5.6).
- **Retriever (Fase 2)** ordena por score desc e curto-circuita query vazia.
  Compat embedder↔coleção fica no boot do `ChromaVectorStore`, não no retriever.
- **Geração (Fase 3)**: `RAGPipeline.ask` faz curto-circuito quando não há contexto
  relevante (não gasta LLM/quota). `FallbackLLM` implementa hybrid (Ollama → Gemini
  se offline); adapter Gemini envolto por `ratelimit` (backoff 429).
- **Citações**: extrai `[n]` da resposta; se o modelo não citar, cai para todos os
  chunks recuperados (rastreável > vazio).
- **Testes sem rede/quota**: fakes para embedder/LLM/retriever; Chroma real (é dep,
  sem rede) para os contratos de store/retrieval.

## 3. Ambiente (gotchas)
- venv em **Python 3.14**; `uv` **não está no PATH** → usar `.venv/Scripts/python.exe`
  e `pip` direto. `chromadb 1.5.9` instala e roda em 3.14.
- Deps da Fase 1 não vinham sincronizadas no venv; instaladas manualmente.

## 4. Estado ao fim
- 42 testes verdes, ruff limpo. Commits: fase-1 `bca4d98`, fase-2 `8985de3`, fase-3 `220a854`.
- **Não verificado**: ponta-a-ponta com Ollama/Gemini reais (exige `make ollama-up`).

## 5. Próximos passos
- **Fase 4** — Frontend Streamlit (demo/GIF). Instalar `streamlit`; DoD é smoke manual.
- **Fase 5** — Avaliação (golden set 30q, Recall@5, tokens, custo). Depende só da Fase 3,
  mais verificável por teste — bom candidato a seguir antes/junto da 4.
- Rodar smoke real com Ollama para fechar DoD de geração das Fases 1-3.
