# Retro — 2026-07-16 · Fases 4→7 (Frontend, Avaliação, Observabilidade, Publicação)

## 1. Objetivo
Continuar do fim das Fases 1–3 e fechar o V1: frontend, métricas, observabilidade e
o material de publicação — automatizando tudo que não depende de máquina/corpus.

## 2. Decisões-chave
- **Fase 4 (Streamlit):** lógica isolada em `app/backend.py` (testável sem UI);
  `@st.cache_resource` por (modo, provider). Smoke via `streamlit.testing.AppTest`
  (Playwright MCP não expôs browser tools) — roda o script in-process e pega exceção.
- **Fase 5 (Eval):** métricas puras (`recall_at_k`, `cost_usd`) + evaluator com relógio
  injetável e **cache de respostas** → rerun com 0 chamadas (determinismo provado em teste).
  `out_of_corpus` = acerto quando NÃO recupera nada (controle de alucinação).
- **Fase 6 (Observabilidade):** `Tracer` Protocol; JSON local por padrão, Langfuse opcional
  (import preguiçoso — não vira dep obrigatória). Modo local **blindado**: factories recusam
  provider de nuvem em `RAG_MODE=local`; teste `local_only` prova que `ask()` não abre socket.
  `friendly_errors` no CLI mapeia Ollama-offline/429/mismatch para mensagem + exit code.
- **Fase 7 (Docs):** README PT alinhado ao código (badge CI real, observabilidade correta),
  README EN, `check_links.py`, e `docs/PUBLISHING.md` com o que resta manual.

## 3. Gotchas
- Python 3.14 no venv; `uv` fora do PATH → `.venv/Scripts/python.exe` + pip. chromadb/
  streamlit/structlog instalam e rodam em 3.14.
- `Settings.model_copy(update=...)` **não coage** str→enum nem re-valida → no teste, construir
  `Settings(...)` direto (com key dummy) em vez de model_copy.
- Emoji em `print()` quebra console cp1252 (Windows) → tags ASCII em scripts.
- CI faz `ruff format --check .` → precisei rodar `ruff format` no repo todo.

## 4. Estado ao fim
- Fases 0–7 implementadas. **70 testes verdes**, ruff check+format limpos, cobertura ~69%
  (gap = adapters de nuvem e UI, só exercitáveis com serviço vivo).
- Commits: fase-4 `336ddf5`, fase-5 `2a5d93d`, fase-6 `4a132c3`, fase-7 `affb6f6`.

## 5. Próximos passos (todos manuais — ver docs/PUBLISHING.md)
- Provar VRAM (RTX 5060 8GB) + smoke real Ollama/Gemini.
- Golden set 30q sobre corpus real → `rag eval` → preencher métricas no README (PT+EN).
- Gravar `docs/assets/demo.gif`; revisar README EN; tornar repo público + post LinkedIn.
